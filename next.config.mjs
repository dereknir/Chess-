/** @type {import('next').NextConfig} */
// after() 在 Next.js >= 15.1 已是穩定 API,不需要 experimental flag。
// app/actions.ts 的 import { after } from 'next/server' 維持不變。
const nextConfig = {};

export default nextConfig;
